$git add .
今いるフォルダの全部を変更します。
「新規ファイル」「修正したファイル」「削除したファイル」など
をまとめてステージング(登録)します。

$git add yoshi.html
そのファイルだけを変更します。



$git commit -m "first commit"


git add	        保存準備
git commit	ローカルに保存
git push	サーバーへ送信